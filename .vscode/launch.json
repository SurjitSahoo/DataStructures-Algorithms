{
  "configurations": [
    {
      "name": "Debug",
      "request": "launch",
      "preLaunchTask": "tsc: build - tsconfig.json",
      "outFiles": ["${workspaceFolder}/out/**/*.js"],
      "skipFiles": ["<node_internals>/**"],
      "type": "node"
    }
  ]
}
